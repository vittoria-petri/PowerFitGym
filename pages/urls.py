from django.urls import path
from exercises import views

# from .views import HomePageView

urlpatterns = [
#    path("", HomePageView.as_view(), name="home"),
#    path("", views.AddBlock, name="AddBlock"),
]
